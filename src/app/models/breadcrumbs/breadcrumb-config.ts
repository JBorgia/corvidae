import {BreadcrumbElement} from '@models/breadcrumbs/breadcrumb-element';

export class BreadcrumbConfig {
  title?: string;
  titleElement?: BreadcrumbElement;
  breadcrumbElements?: BreadcrumbElement[];
}
